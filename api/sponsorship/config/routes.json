{
  "routes": [
    {
      "method": "GET",
      "path": "/sponsorships",
      "handler": "sponsorship.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/sponsorships/count",
      "handler": "sponsorship.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/sponsorships/:id",
      "handler": "sponsorship.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/sponsorships",
      "handler": "sponsorship.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/sponsorships/:id",
      "handler": "sponsorship.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/sponsorships/:id",
      "handler": "sponsorship.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
